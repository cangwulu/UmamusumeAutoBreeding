# ============================================================
# UmamusumeAutoTrainer 依赖清单（顶层约束）
# 目标解释器: Python 3.11.x (64位, Windows)
# 说明:
#   1) 本文件记录"版本意图"，可直接被 pip 安装。
#   2) 首次安装完成后, install.ps1 会自动生成全量锁定快照
#      requirements.lock.txt（含全部传递依赖），此后安装以它为准，
#      保证可复现。lock 文件应随本文件一起入库。
#   3) 不要在本文件里放开 numpy>=2: paddlepaddle==2.6.2 是 numpy 1.x 时代产物。
# ============================================================

# 必须先装老版 setuptools，否则 adbutils 会因 pkg_resources 缺失报错
setuptools<70

# ========== OCR 全家桶（定海神针，精确钉死=本机 conda uat 验证过的组合，勿动）==========
# 注意: paddleocr 用 >=2.7,<3 会被 pip 解析到 2.10 并触发大量依赖回溯升级(albumentations/
# scipy/opencv-headless...), 故精确钉死 ==2.7.0.2; opencv 同样钉死本机版本防升级。
paddlepaddle==2.6.2
paddleocr==2.7.0.2
numpy>=1.21,<2
opencv-contrib-python==4.6.0.66
Pillow==10.0.0

# ========== Android 控制 / 设备 ==========
uiautomator2
adbutils
psutil
plyer

# ========== Web 服务 / 调度 ==========
fastapi
uvicorn
pydantic
starlette
croniter
colorlog
colorama
pytz

# ========== 网络 / 数据 ==========
PyYAML

# 注: 代码真实依赖已按 import 扫描精简 (2026-09-02 M0)。
# 工具脚本(tools/*.py)与 BWIKI 抓取均用标准库 urllib, 无需 requests/bs4/lxml。
